[package]
name = "aislap"
version = "0.6.0"
description = "aiSLAP - AI desktop pipeline"
authors = ["Piotr"]
edition = "2021"

[lib]
name = "aislap_lib"
crate-type = ["staticlib", "cdylib", "rlib"]

[profile.release]
lto = true
codegen-units = 1
strip = true
opt-level = "s"

[build-dependencies]
tauri-build = { version = "2", features = [] }

[dependencies]
tauri = { version = "2", features = ["protocol-asset"] }
tauri-plugin-dialog = "2"
tauri-plugin-fs = "2"
tauri-plugin-window-state = "2"
tauri-plugin-http = "2"
tauri-plugin-updater = "2"
tauri-plugin-process = "2"
serde = { version = "1", features = ["derive"] }
serde_json = "1"
reqwest = { version = "0.12", features = ["rustls-tls"], default-features = false }
tracing = "0.1"
tracing-subscriber = "0.3"
chrono = { version = "0.4", features = ["serde"] }
dirs = "5"
thiserror = "1"
base64 = "0.22"
regex = "1"
img-parts = "0.4"
bytes = "1"
sha2 = "0.10"
libsql = "0.9"
uuid = { version = "1", features = ["v4"] }
imagesize = "0.15"
# Thumbnail encoding only. Feature set mirrors `fsutil::IMAGE_EXTS`; the PNG
# decoder is also what reads the legacy 16-bit `rgb48be` posters during a
# backfill. `imagesize` above stays — it reads dimensions from the header
# without decoding, which is a different and much cheaper job.
image = { version = "0.25", default-features = false, features = ["jpeg", "png", "webp"] }
whoami = "1"

[dev-dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
